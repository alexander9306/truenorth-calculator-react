import { Typography } from '@mui/material';

const Loading = () => {
  return (
    <Typography mt={2} color="steelblue" variant="h3">
      Loading...
    </Typography>
  );
};

export default Loading;
